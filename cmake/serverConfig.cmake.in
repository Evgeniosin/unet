@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# spdlog is only needed in Debug builds
if("@CMAKE_BUILD_TYPE@" STREQUAL "Debug")
    find_dependency(spdlog REQUIRED)
endif()

check_required_components(server)

