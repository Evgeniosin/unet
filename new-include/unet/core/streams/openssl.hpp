#pragma once

#include <uvent/Uvent.h>

namespace usub::unet::core {

    template<typename Dispatcher, typename Policy>
    class OpenSSLStream {
    public:
        OpenSSLStream() = default;
        ~OpenSSLStream() = default;


    private:
    };
}// namespace usub::unet::core